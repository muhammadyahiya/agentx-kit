"""Shared building blocks for {{ spec.slug }}.

``libs/`` holds framework-level helpers that are not tied to one agent:
logging setup, the agent/worker factory, {% if spec.use_voice %}voice I/O, {% endif %}and other reusable glue.
"""
